'use client';
import { useDashboardData } from '@/components/DataClient';
export default function Activities(){ const {data,error,loading}=useDashboardData(); if(loading)return <p>Loading...</p>; if(error)return <p className="error">{error}</p>; const rows=data.activities.slice(0,300); return <><h1>Activities</h1><p className="small">Showing first 300 activities for performance. API contains full Excel source.</p><div className="table-wrap section"><table><thead><tr><th>ID</th><th>Name</th><th>Phase</th><th>Planned Start</th><th>Planned Finish</th><th>Actual Start</th></tr></thead><tbody>{rows.map((r:any,i:number)=><tr key={i}><td>{r['Activity ID']}</td><td>{r['Activity Name']}</td><td>{r.Phase}</td><td>{r['Planned Start']}</td><td>{r['Planned Finish']}</td><td>{r['Actual Start']}</td></tr>)}</tbody></table></div></> }
